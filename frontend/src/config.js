export const config = {
    // Quando você subir o backend para o Vercel, substitua 'http://localhost:3000' 
    // pela URL que o Vercel te fornecer (ex: https://seu-backend.vercel.app)
    apiUrl: 'https://sermoessaas.vercel.app/'
};
